
import { Property } from './types';

// FIX: Added MAX_PRICE constant for the price filter to resolve import error in PropertyFilter.tsx
export const MAX_PRICE = 30000000;

export const INITIAL_PROPERTIES: Property[] = [];
